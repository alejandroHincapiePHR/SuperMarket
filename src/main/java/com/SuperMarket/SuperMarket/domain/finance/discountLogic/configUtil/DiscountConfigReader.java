package com.SuperMarket.SuperMarket.domain.finance.discountLogic.configUtil;
import java.io.FileInputStream;
import java.io.IOException;
import java.util.Properties;

public class DiscountConfigReader {/*
    private String parametersChain;
    public DiscountConfigReader() {
    }
    public String[] loadConfig(String configFile, String productType) {
        Properties prop = new Properties();
        try (FileInputStream input = new FileInputStream(configFile)) {
            prop.load(input);
            parametersChain = prop.getProperty(productType);
            return parametersChain.split(",");
        } catch (IOException ex) {
            ex.printStackTrace();
            return null;
        }
    }
    */
}
